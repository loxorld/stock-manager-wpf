<Window x:Class="StockManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:StockManager.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Titulo}"
        Height="250" Width="420">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="16">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="260">
            <TextBlock Text="{Binding Titulo}" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12" />
            <TextBlock Text="{Binding Contador}" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <Button Content="Incrementar"
                    Command="{Binding IncrementarCommand}"
                    Height="40"/>
        </StackPanel>
    </Grid>
</Window>
